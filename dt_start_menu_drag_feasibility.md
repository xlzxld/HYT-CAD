# dt_start.lsp 菜单"拖拽重排"可行性报告

- 分析对象：`scripts/dt_start.lsp`（v3.4，版本串见 L98；本报告只读该文件，未读其它源文件）
- 日期：2026-09-12
- 需求：用户**按住鼠标左键长按一级/二级菜单项后随意拖动**，实现"提升为一级菜单 / 降为二级菜单 / 同级内调序"
- 结论一句话：**"鼠标拖拽"这个交互形式在现有结构内不可行（无输入通道）；但"调整菜单层级与顺序"这个能力本身可行，代价是把交互换成命令行或对话框，且需要改造数据驱动层与持久化层。**

---

## 1. 术语对齐（先说清楚"一级/二级"）

`dt_start` 的实际层级与日常说法有一层错位，后文一律用本表口径：

| 日常说法 | dt_start 实际位置 | 代码证据 |
|---|---|---|
| 顶栏按钮（"热流道自动化(R)"） | 菜单栏上的 popup | L100 标题、L621-623 `vla-insertinmenubar` |
| 用户说的**一级**（顶层可拖的项） | 顶栏 popup 的**直接子项**：分流板/加热条/出线槽/外协加工/工具 五个子菜单 + 关于 | L577-606 构建循环 |
| 用户说的**二级**（子菜单里的项） | 子菜单内的菜单项：如"画分流板/分流板参数"、"线切割/精雕/数据图纸" | L582-590 |

即：用户想要的"一级 ↔ 二级互转"，本质是**菜单项的换父节点（re-parent）**；"同级调序"是**换索引**。

---

## 2. 现状结构拆解（可行性判定的地基）

| 事实 | 证据 | 对需求的影响 |
|---|---|---|
| 菜单是 **COM 运行期内存构建**，不落盘、不动主 CUI | L12、L473、L543-627 | 菜单本身不持久化，"拖拽结果"必须另行落盘 |
| 二级分组顺序 = 家族表行序 | L92-96 `dt:st-families`、L14 注释 v2.7 | "同级调序"在二级层已是**现成能力**，改表即可 |
| 三级菜单项**硬编码**在构建函数里（外协加工 5 项、每个家族 2 项） | L582-590 | 任意结构调整必须先把硬编码改成数据驱动 |
| 每个二级项都带**字母热键**，且约定"同一层不可重复" | L90 注释、L93-96 第五列、L579 | 提升/降级后热键可能撞车，需自动去重 |
| 2024 已移除 `MenuGroups.Add`，改用 **ACAD 主菜单组**承载 | L55-60（v2.3）、L553-562 | 重排是在**用户原生菜单组上动刀**，安全边界必须是白名单 |
| popup 建后 `Delete` 被拒，会话内只完整构建一次 | L57-60（v2.3）、L570-571、L631-619 | **会话内热重建不可靠**，这是最大的实现障碍 |
| 摘除逻辑只删"自建组内 popup"或"与顶栏标题同名的 popup" | L508-534 | 若允许升级为**独立顶栏 popup**，卸载逻辑必须同步扩展，否则残留 |
| 项目已具备读写外部文件的原语与先例 | L295-337（`open`/`write-line`/`read-line`）、L285-291 转义、`scripts/` 下 7 个 `.ini` | 持久化"顺序/层级"技术上没有障碍 |
| 项目已在使用 DCL 做对话框 | L733-777（`dt:find-dcl` / `load_dialog` / `new_dialog` / `set_tile`） | 方案 A 的交互载体已有成熟先例 |
| 文件必须 **UTF-8 with BOM**，并有 GBK 副本门禁 | L84、AGENTS.md §2 构建命令 | 新增含中文的配置文件要处理编码双轨 |

---

## 3. 逐条判定

### 3.1 "鼠标长按拖动菜单项" —— ❌ 不可行（置信度：确凿）

三条独立理由，任一条即可否决：

1. **菜单栏是 AutoCAD 原生窗口，不向 AutoLISP 暴露输入**。`dt_start` 全篇菜单代码只有"构建 / 查询 / 摘除"三类调用（L508-627），没有任何输入事件通道；AutoLISP 的反应器体系面向数据库、文档、命令、编辑器、系统变量等对象级事件，**不含菜单项级鼠标事件**。
2. **DCL 做不到**。项目现用的 DCL（L733-777）只有 `action_tile` 之类的值变化回调，**没有鼠标移动/按下/释放事件**，因此即使用对话框重画一个"菜单列表"，也无法在 DCL 里实现拖拽。
3. **`grread` 之类命令行/绘图区鼠标通道与菜单栏无关**，无法用于监听顶栏菜单上的拖动。

> 若坚持"就是要在屏幕菜单/面板上拖"，只剩两条路：引入第三方对话框库（OpenDCL 一类，其列表/树控件带拖放事件），或用 .NET/ObjectARX 自建面板。两条都**引入第三方依赖或编译产物**，与项目"纯 AutoLISP + 无第三方依赖 + 2007~2026 全版本通用"的技术栈（AGENTS.md §2）直接冲突，且按 R-3.5 必须人工授权。

### 3.2 "调整层级与顺序"（交互换成命令/对话框） —— ✅ 可行（置信度：确凿）

需求真正想达成的是"能改结构、能调序"，这一层完全落得进现有结构：

- **二级同级调序**：几乎零改造，家族表行序即菜单序（L14、L92-96），改行序 + 落盘即可。
- **结构调整（升级/降级）**：需要把"家族表"升级为**菜单树**（节点 = 标题/宏/热键/父节点/序号），构建函数改成遍历这棵树。
- **持久化**：把树写进脚本目录里的一个配置文件，下次引导时读回；读不到或损坏就回落到内置默认顺序（保证菜单**永远能出来**）。

### 3.3 "改完立即在当前会话生效" —— ⚠️ 受限（置信度：疑似，需实测）

v2.3 的实测结论很关键：**popup 建后 `Delete` 静默失败**，所以现有架构走了"会话内只完整构建一次 + 残留复用"的路子（L570-571、L607-617）。由此推出两种可能，**必须实测才能定档**：

- 若菜单**项**（非 popup）可以删除并重新插入到指定索引 → 可做"实时重排 + 失败回落提示重启"；
- 若菜单项同样删不掉 → 只能"写盘 → 提示重启 CAD 生效"，交互体验降一档，但功能仍完整。

**待实测项（建议一条最小探针命令验证，不必写完整功能）**：在 2024 上对 ACAD 组里自建 popup 的一个菜单项调用删除 + 指定位置重插，观察命令行是否报错、菜单是否变化。

---

## 4. 实现思路（推荐 A，备选 C）

**方案 A（推荐，零新依赖）**：菜单里挂一项"菜单管理…" → 打开 DCL 对话框，用一个列表框展示当前树 + 上移/下移/升级/降级四个按钮 → 写配置文件 → 尝试热重建，失败则提示"重启 CAD 生效"。
- 优点：完全在现有技术栈内；DTUNINSTALL / 幂等逻辑改动面小。
- 缺点：**不是用户原本设想的拖拽手感**，是多点两下。

**方案 B（不采纳）**：劫持原生菜单拖拽 —— 见 §3.1，无 API。

**方案 C（可行但重）**：引入 OpenDCL / .NET 面板换取真正的拖放手感。
- 代价：新增第三方依赖（R-3.5 需授权）、需在每台机器部署运行库、2007~2015 老版本支持度下降、卸载流程要新增一步。
- 适用前提：用户明确接受"手感优先于零依赖"，且分发对象机器可控。

**方案 D（架构级替代，供参考）**：把菜单从"运行期 COM 内存构建"改为**加载一份局部 CUIx（partial customization）**。这样用户可以直接用 AutoCAD 自带的 CUI 编辑器拖拽改结构，且**原生跨会话持久**。
- 代价：直接推翻 L12 的既有设计决策（不落盘、不动主 CUI）；DTUNINSTALL 要新增"卸载局部 CUI"步骤；菜单项与脚本的绑定方式需重做。
- 只有当"用户自助改菜单"是长期高频需求时，这个改造才划算。

---

## 5. 关键技术点

1. **数据驱动改造（前置必做）**：把 L582-590 里硬编码的三级项抽成菜单树数据；否则任何结构调整都要改代码。
2. **持久化与编码**：新配置文件若含中文，要同时满足 UTF-8 with BOM 与 `scripts_ansi` GBK 双轨约定（L84 + AGENTS.md §2）。**更稳的做法是配置里只存 ASCII 键名/序号、中文标签仍从代码取**，从源头绕开编码坑。写入要用 `dt:st-2bs` 同类的转义思路（L285-291），避免生成被再次读取时出错的字面量。
3. **安全边界（红线级）**：因为菜单挂在 **ACAD 主菜单组**（L553-562），任何重排/删除都必须限定在"我们自己建的项"白名单内，**不得触碰 AutoCAD 原生菜单**。建议用 Tag/命名前缀做归属标记，构建与删除都按标记筛选。
4. **卸载一致性**：若允许"升级为独立顶栏 popup"，`dt:st-menu-remove`（L508-534）必须扩展识别范围，否则 DTUNINSTALL 会漏删、残留菜单。
5. **热键去重**：每层内的字母热键不能重复（L90）。升降级后需自动重新分配，否则 `Alt` 快捷键失效或指向错项。
6. **会话重建限制**：见 §3.3，是体验上限的决定因素。
7. **幂等与残留检测适配**：L607-617 的"残留菜单宏格式抽查"逻辑，会因为菜单内容被用户改过而误判，需同步调整。
8. **失败回落**：配置文件缺失/损坏/权限不足，都必须回落到内置默认顺序，绝不能出现"菜单出不来"。

---

## 6. 潜在限制与风险

| # | 风险 | 等级 | 影响 | 缓解 |
|---|---|---|---|---|
| 1 | 原生拖拽不可实现，只能降级为对话框/命令交互 | 高 | 需求核心体验打了折扣 | 提前与用户对齐"能改结构"≠"能拖动" |
| 2 | 会话内删菜单被拒 → 必须重启生效 | 中 | 改完看不到变化，易被误认为没生效 | 明文提示"重启 CAD 生效"，或实测后尝试热重建 |
| 3 | 误删/污染 ACAD 原生菜单 | 高 | 用户 CAD 界面异常，卸载也难复原 | 严格白名单 + 归属标记；先 dry-run 打印将要改的项 |
| 4 | 配置文件损坏 / 乱码 | 中 | 菜单挂不出或中文乱码 | 存 ASCII 键名；解析失败即回落默认；写入前备份旧配置 |
| 5 | 引入 OpenDCL/.NET 破坏"纯 LISP 无依赖"约束 | 中高 | 分发、老版本兼容、卸载复杂度全面上升 | 默认走方案 A；方案 C 需 R-3.5 显式授权 |
| 6 | 热键重复 | 低 | 个别 Alt 快捷键失效 | 自动重新分配 + 冲突检测 |
| 7 | 与用户在 CUI 编辑器的改动互相覆盖 | 低 | 认知混乱 | 明确"运行期菜单不写 CUIx，重启即回归" |
| 8 | 多副本环境下 `findfile` 命中旧副本（项目已知坑 #35） | 低 | 配置读错目录 | 沿用 `*dt-script-dir*` 确定性目录注入（L144-148） |

---

## 7. 落地时必须跑的门禁（AGENTS.md §2）

任何对 `dt_start.lsp` 的实际改动，交付前必须实测通过：

- 静态检查：`python tools/check_lisp.py scripts/dt_start.lsp`
- 构建：`python tools/make_ansi.py`（生成 `scripts_ansi` GBK 副本并逐字节回读校验）
- 测试：`python tools/test_direction.py && python tools/check_audit_fixes.py`
- 真实菜单行为（拖拽/重排属交互变更，不可只靠静态检查）：需在目标 CAD 版本上人工执行一遍挂菜单、重排、卸载全流程，并留命令输出取证。

---

## 8. 待实测确认清单（本报告标"疑似"的项）

1. ACAD 组内自建 popup 的**菜单项**能否删除、能否重插到指定索引（决定"实时生效"还是"重启生效"）。
2. AutoCAD 2024 上是否存在任何可订阅的菜单变更通知（用于自动同步，非必需）。
3. DCL 列表框在实际版本上能否支撑"上移/下移"按钮式重排的交互手感（功能可行，手感需实测）。
4. 老版本（2007~2015）上配置文件读写路径与权限是否一致。

---

## 9. 边界声明（本次不做的事）

- 不给出任何完整代码或操作教程；
- 未改动 `dt_start.lsp` 或任何源文件，本报告为纯只读分析；
- 本报告只基于 `dt_start.lsp` 单文件证据，未核对 `flb_runner` / `jrt_runner` 等家族脚本的内部实现（其对结论无实质影响）。
